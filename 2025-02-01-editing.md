---
title: 編撰文章的方式
date: 2025-02-01
tags: 
  - 技術
  - 網站
  - 文章
  - 永久性草稿
  - 內部用途
draft: true
hidden: true
---

此文章用於介紹如何編寫 I. 字坊博客下的文章。此博客由 GitHub Pages （Jekyll）服務。

<style>code{color: color-mix(in srgb, var(--secondary-colour) 40%, var(--text-colour));}</style>

## 結構

### 文件名

用於編寫文章的文件是 Markdown 格式，一般使用 `.md` 後綴。文章必須存放於此文件夾（`_posts`）內。所有文章在瀏覽時會在 `/blog/` 路徑下，如 `https://ichitenfont.github.io/blog/`。

文件名必須以日期（年-月-日-）起始，然後加上簡單的辨識字符串；辨識字符串將會用於生成對應的網址。

### 前言信息 (frontmatter)

文件內的開始部分是關於文章的前言信息，在一對三個橫綫 `---` 內加上標題（title）、日期（date）、標籤（tags）等信息。下面提供最簡單的例子，使用的是本文章的前言信息。

```yml
---
title: 編撰文章
date: 2024-04-01
tags: 
  - 技術
  - 網站
  - 文章
  - 永久性草稿
  - 內部用途
---
```

前言信息必須在文件的最開始部分。文件名的日期和前言信息的日期不一樣沒關係，但是建議保持一樣避免混淆。

### 草稿

如果想要在[文章列表](/blog)內隱藏某個文章（如此文章），在前言信息裏加上兩個信息標籤：
```yml
draft: true
hidden: true
```
網頁仍舊可以使用下面的網址解析方式訪問，但是不會公開顯示。另外，使用這兩個信息標籤也會分別在文章加上「草稿」和「非公開」標籤（tag）。

### 網址解析

文章的網址是以 *前言信息* 的日期和文件名的辨識字符串組合，例如此文章是 `2024-04-01-editing.md` 會對應至網址 `/blog/2024/04/01/editing`。

如果文件名的辨識字符串有空格，網址內會替換成連接號 `-`，如 `"2024-04-01-test article.md"` 會對應至網址 `/blog/2024/04/01/test-article`。

## 內容

GitHub Markdown 接受的格式標籤（如單下劃綫 `_` _標重點_，雙星 `**` **加粗**，[`[超鏈接](#)`](#)，`>` 引用）都可以使用，詳情可閱讀 [GitHub Flavored Markdown 格式定義](https://github.github.com/gfm/)。

> 單星 `*`／單下劃綫 `_` 對應標識重點，GitHub Markdown 以斜體顯示，但因中文斜體不符合中文排版習慣，因此改用下點形式標記重點。如果需要使用英文單字且斜體標重點，可以使用 `<em lang='en'>word</em>` 標示該單字是英文重點（emphasise）內容，網頁將會改爲使用斜體顯示，如 <em lang='en'>emphasising these words</em>。英文斜體目前調用 Google Fonts 的 [Taviraj](https://fonts.google.com/specimen/Taviraj)。

避免使用 1 級標題（`# 標題` 或者 `<h1>標題</h1>`），該級用於頁頭的文章標題。

### 摘要

在 `/blog/` 瀏覽全部文章時，各文章會顯示標題，日期和摘要。文章摘要是采用文件前言信息之後的第一段文字（一段後面有空行）。

如果需要特別選擇開頭特定範圍的內容爲摘要，可以在前言信息加上 `excerpt_separator` 標示切斷範圍，如下：
```md
---
excerpt_separator: <!--more-->
---

Excerpt with multiple paragraphs

Here's another paragraph in the excerpt.
<!--more-->
Out-of-excerpt
```

### 切換字型

本頁面引用了「一點明體」、「一點明體CP」、「一點明體 異體」和「一點明體 異體CP」四個網頁字型。如果文章內需要切換特定字體，可使用以下格式：
* 一點明體：默認使用，不提供切換。
* 一點明體 CP：`<iming-cp>文字</iming-cp>`
* 一點明體 異體：`<iming-var>文字</iming-var>`
* 一點明體 異體CP：`<iming-varcp>文字</iming-varcp>`

一般情況應該不需要使用CP字型。例如下面引用部分異體漢字（見於《一點明體功能記錄》）：
```html
<iming-var>華朝真直憲獵鼬契湱以奐沿朁詩弒你住瞭票飄尉慰衰縗肖削吳誤鋁躳罪羅畚騁蔑愛茶益甚亟殛常夜䘸派穴步癸赹慈</iming-var>
```

> 正體：華朝真直憲獵鼬契湱以奐沿朁詩弒你住瞭票飄尉慰衰縗肖削吳誤鋁躳罪羅畚騁蔑愛茶益甚亟殛常夜䘸派穴步癸赹慈  
> 異體：<iming-var>華朝真直憲獵鼬契湱以奐沿朁詩弒你住瞭票飄尉慰衰縗肖削吳誤鋁躳罪羅畚騁蔑愛茶益甚亟殛常夜䘸派穴步癸赹慈</iming-var>

### 未編碼漢字——倉庫內上傳圖片

如果需要以圖片形式顯示未編碼漢字，可使用
<!-- {% raw %} -->
```html
<img class="picture-char" alt="[IDS序列/解釋]" src="{{page.images}}/char/[文件名].svg">
```
<!-- {% endraw %} -->
引用漢字圖片，如→→→<img class="picture-char" alt="「关」的異體字" src="{{page.images}}/char/关異.svg">←←←是「关」的異體字。

注意文檔內純文本的 `src` 圖片來源需要輸入 <!-- {% raw %} -->`{{page.images}}` 前後的兩個 `{{}}`<!-- {% endraw %} -->。

對應的圖片可放置於此文件夾內的 `images`（`_posts/images`），後面加上斜杠 `/` 然後繼續文件夾內的路徑，例如未編碼漢字放置於 `images/char`（`_posts/images/char`）。此網站開發時已經特別設置文章內容裏面的 <!-- {% raw %} -->`{{page.images}}`<!-- {% endraw %} --> URL 標籤會對應 `images` 文件夾內的圖片，因此只需要如上引用即可。

爲了照顧無障礙使用者和圖片缺失的情況，請務必給文字圖片加上 `alt` 屬性，以純文本描述該圖片顯示的文字，也可在複製該圖片文字時自動替換成 `alt` 屬性的文字。此處可以只放簡單的句子，如上面的「『关』的異體字」，或者使用 [IDS 描述序列](https://zh.wikipedia.org/wiki/%E8%A1%A8%E6%84%8F%E6%96%87%E5%AD%97%E6%8F%8F%E8%BF%B0%E5%AD%97%E7%AC%A6)。下表提供完整的描述符號（半包圍列出開口方向）：

|類型|符號|
|---:|:---|
|左右|⿰ 左右、⿲ 左中右|
|上下|⿱ 上下、⿳ 上中下|
|全包圍|⿴|
|半包圍兩邊|⿹ 左下（気）、⿸ 右下（𤴧）、⿽ 左上（𠥼）、⿺ 右上（赵）|
|半包圍三邊|⿶ 上（凶）、⿵ 下（冈）、⿼ 左（㕚）、⿷ 右（区）|
|重叠|⿻|
|減法|㇯|
|一元变化|⿾ 水平翻轉、⿿ 旋轉180°|
|其他|〾 形似但不相等|


### 未編碼漢字——其他網站

如果要使用其他網站的漢字，也可使用
```html
<img class="picture-char" alt="[IDS序列/解釋]" src="[網址]">
```
引用其他網站的漢字圖片，只需要保證 `class="picture-char"` 存在即會當作漢字圖片。漢字圖片應該是黑色字，透明背景，黑暗模式下網站會自動反白圖片。

例如使用 [GlyphWiki](https://glyphwiki.org) 的 [UTC-00949（招財進寶）漢字](https://glyphwiki.org/wiki/utc-00949)圖片：
```html
<img class="picture-char" alt="招財進寶合字" src="https://glyphwiki.org/glyph/utc-00949.svg">
```
→→→<img class="picture-char" alt="招財進寶合字" src="https://glyphwiki.org/glyph/utc-00949.svg">←←←，或者當前 [GlyphWiki 沙箱的漢字](https://glyphwiki.org/wiki/sandbox)：
```html
<img class="picture-char" alt="GlyphWiki 沙盒漢字" src="https://glyphwiki.org/glyph/sandbox.svg">
```
→→→<img class="picture-char" alt="GlyphWiki 沙盒漢字" src="https://glyphwiki.org/glyph/sandbox.svg">←←←。使用 GlyphWiki 的鏈接是單擊對應漢字頁面的 `(SVG圖像)` 鏈接，然後複製打開的網址即可。

![GlyphWiki 內的 (SVG圖像) 鏈接]({{page.images}}/pic/glyphwiki-sandbox.png)

或者使用 [字統網](https://zi.tools) 的 IDS 動態組字 API。例如需要顯示 `⿰文字`，可以呼叫 `http://zu.zi.tools/⿰文字.svg`：
```html
<img class="picture-char" alt="⿰文字" src="http://zu.zi.tools/⿰文字.svg">
```
→→→<img class="picture-char" alt="⿰文字" src="http://zu.zi.tools/⿰文字.svg">←←←。此功能依賴於字統網，若該網站出現服務問題可能導致顯示失敗。

### 普通圖片

圖片也可使用 <!-- {% raw %} -->`{{page.images}}`<!-- {% endraw %} --> URL 標籤對應 `images` 文件夾內容。有兩種方式：使用 GitHub Markdown 的感嘆號超鏈接格式： 
<!-- {% raw %} -->
```md
![image]({{page.images}}/pic/[文件名])
```
<!-- {% endraw %} --> 
或者使用 HTML 的 `<img>` 圖片標籤：
<!-- {% raw %} -->
```html
<img alt="image" src="{{page.images}}/pic/[文件名]">
```
<!-- {% endraw %} -->
此處不需要加上 `class="picture-char"` 以避免誤認爲漢字。上面 GlyphWiki 的圖片引用方式使用感嘆號格式插入，同樣需要注意 <!-- {% raw %} -->`{{page.images}}` 前後的兩個 `{{}}`<!-- {% endraw %} -->。內文圖片建議放入此文件夾的 `images/pic/` 文件夾內。

如果普通圖片想要在切換主題時反轉顏色，可在 `<img>` 加上 `class="theme-invertible"`，這會在黑暗模式時給圖片加上反色蒙版。建議原始圖片爲白底黑字，切換后會變成黑底白字。需注意反轉后的顏色會出現偏差，如果使用主題色則不會出現偏差，因此建議若要讓圖片依據主題顏色切換，請在圖片裏使用黑灰白和主題色。下面的圖片會依據主題顏色切換。

<img alt="image" src="{{page.images}}/pic/glyphwiki-sandbox.png" class="theme-invertible">

本站的主題色是 <span id="colour-hex-code" style="color: var(--theme-colour);"></span>。
<script>
// 提取 CSS 的主題色
document.getElementById("colour-hex-code").innerHTML = getComputedStyle(document.documentElement).getPropertyValue('--theme-colour');
</script>

## 技術性資訊

### GitHub 倉庫部署

爲了方便維護主網站和博客，因此采用兩個不同的倉庫維護。主網站存放於 `ichitenfont.github.io` 倉庫內，而博客文章和相關圖片放在 `blog` 倉庫內。`blog` 倉庫通過 `git submodule` 功能鏈接進入 `ichitenfont.github.io` 倉庫內，方便直接導出同一個網站。`blog` 倉庫的更新會自動讓  `ichitenfont.github.io` 倉庫同步更新，編者一般情況下只需要增修 `blog` 倉庫內容即可。

### 技術棧 (tech stack)

本網站使用 GitHub Pages 自帶的 Jekyll 博客功能製作。所有頁面引用自製的模板套用外皮。另外博客也使用 `jekyll-feed` 生成 Atom (RSS) 摘要，可於 [{{site.url}}/blog/feed.xml]({{site.url}}/blog/feed.xml) 接收。

### 特殊標籤

本文章的純文本裏面有部分 `<!--{{ "{% raw " }}%}--><!--{{ "{% endraw " }}%}-->`，是用於避開 GitHub Pages 的 Jekyll 預處理器將 <!-- {% raw %} -->`{{page.images}}`<!-- {% endraw %} --> 展開解讀成 `{{page.images}}`。普通情況下並不需要使用這個 `% raw %` 標籤。

如果某段落需要特殊的 CSS class，可以使用 Jekyll Kramdown 的「段落行內特性列表」(<em lang="en">Block Inline Attribute Lists</em>)，格式爲 `{: CSS_SELECTOR}`。例如上面異體切換也可以通過 CSS `.iming-var` class 使用：
```md
{: .iming-var }
→→→異體：華朝真直憲獵鼬契湱以奐沿朁詩弒你住瞭票飄尉慰衰縗肖削吳誤鋁躳罪羅畚騁蔑愛茶益甚亟殛常夜䘸派穴步癸赹慈←←←
```

{: .iming-var }
→→→異體：華朝真直憲獵鼬契湱以奐沿朁詩弒你住瞭票飄尉慰衰縗肖削吳誤鋁躳罪羅畚騁蔑愛茶益甚亟殛常夜䘸派穴步癸赹慈←←←

需要注意這個特性是針對一整個段落應用，而不是一小段文字。此功能可用於給標題添加點橫下劃綫（CSS `.theme-underline .theme-underline-fit-text` class）如下：
```md
{: .theme-underline .theme-underline-fit-text}
###### 6級標題，帶點橫下劃綫
```

{: .theme-underline .theme-underline-fit-text}
###### 6級標題，帶點橫下劃綫

一般情況並不需要使用此功能，各頁面的標題應該已經設置了正確的下劃綫。